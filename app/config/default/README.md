# Default Config

This directory contains the default config files.
Copy the files to the parent directory and remove the `_default_` from the file names.
Sensitive config settings can be added to the new files, as they will be ignored by version control. 